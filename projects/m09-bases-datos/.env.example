# Copia a .env (nunca commits .env con secretos reales).
POSTGRES_USER=agenda
POSTGRES_PASSWORD=cambia_esto_en_local
POSTGRES_DB=agenda_ops
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Rol de aplicación (L19) — distinto del superuser / owner de migraciones
APP_DB_USER=agenda_app
APP_DB_PASSWORD=cambia_esto_app
